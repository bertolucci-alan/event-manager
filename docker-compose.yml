version: '3'

services:
  event-manager:
    container_name: event-manager
    image: bertoluccialan/event-manager
    build: .
    command: npm run dev
    ports:
      - 3000:3000
    volumes:
      - .:/usr/app
      - /usr/app/node_modules
    environment:
      NODE_ENV: dev
      PORT: 3000
